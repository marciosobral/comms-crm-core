# HTTPS certificates are issued automatically for {$DOMAIN}.
{$DOMAIN} {
	encode zstd gzip

	# API under /api, prefix stripped (the API itself has no /api prefix).
	handle_path /api/* {
		reverse_proxy api:3001
	}

	handle {
		reverse_proxy web:3000
	}
}
